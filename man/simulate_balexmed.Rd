% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate_balexmed}
\alias{simulate_balexmed}
\title{Simulate data from the balance mediation model}
\usage{
simulate_balexmed(
  n = 100,
  d = 50,
  alpha = 1/3,
  beta = 2,
  gamma = 1/3,
  n_plus = 3,
  n_minus = 3,
  rho = 0.2,
  var_log = 9
)
}
\arguments{
\item{n}{Number of samples.}

\item{d}{Number of parts.}

\item{alpha, beta, gamma}{Path coefficients: balance to mediator, mediator to
outcome, and balance to outcome.}

\item{n_plus, n_minus}{Numbers of parts in the numerator and denominator.}

\item{rho}{Correlation parameter of the log-abundances.}

\item{var_log}{Variance \eqn{v} of each log-abundance.}
}
\value{
A list with \code{X} (compositions), \code{m}, \code{y}, \code{covariates} (a data
frame), the true configuration \code{z}, the true \code{balance}, and \code{effects}
(the true direct and indirect effects).
}
\description{
Generates data following the simulation design of the accompanying paper.
Log-abundances are multivariate normal with covariance
\eqn{\Sigma_{jj'} = v \rho^{|j - j'|}}; they are exponentiated and rounded
to counts, zeros are replaced by 0.5, and each row is closed to sum to one.
The first \code{n_plus} parts form the numerator and the next \code{n_minus} parts the
denominator. One standard normal covariate enters both regressions with
coefficient 1, and both error terms are standard normal.
}
\examples{
set.seed(1)
sim <- simulate_balexmed(n = 100, d = 30)
str(sim, max.level = 1)
}
